const letter = require('./letter');

const Word = (letterArray) => {
    this.letterArray = [];
    this.wordString = () => {
        this.letterArray.join("");
        for (let i = 0; i < this.letterArray.length; i++){
            
        } 
    }
}